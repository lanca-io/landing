.success {
	display: flex;
	flex-direction: column;
	align-items: center;
	justify-content: center;
	gap: var(--space-4);
}

.success__button {
	width: 100%;
}

.success__button button {
	width: 100%;
}
