.benefit-panels {
	display: flex;
	flex-direction: row;
	gap: var(--space-3);
	width: 100%;
	max-width: 1920px;
	margin: 0 auto;
}

@media (max-width: 992px) {
	.benefit-panels {
		display: flex;
		flex-direction: column;
		gap: var(--space-5);
	}
}
