.benefit-panels {
	display: flex;
	flex-direction: row;
	gap: var(--space-3);
	width: 100%;
}

@media (max-width: 1280px) {
	.benefit-panels {
		display: flex;
		flex-direction: column;
		gap: var(--space-5);
	}
}
