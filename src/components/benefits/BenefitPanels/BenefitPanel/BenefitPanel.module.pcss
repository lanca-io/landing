.benefit-panel {
	display: flex;
	flex-direction: column;
	gap: var(--space-5);
	max-width: 596px;
}

.benefit-panel__heading {
	display: flex;
	flex-direction: column;
	gap: var(--space-2);
}

.benefit-panel__subtitle {
	max-width: 389px;
}